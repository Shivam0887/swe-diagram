# MongoDB connection — used by the editor/dashboard persistence layer.
# Local dev: spin up `mongod` (or `docker run -p 27017:27017 mongo:7`)
# and use the default URI below.
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=agentic_diagrams
